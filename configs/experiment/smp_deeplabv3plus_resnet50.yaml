# @package _global_
# DEPRECATED flat alias — kept for backward compatibility.
# Prefer: python train.py +experiment=baselines/smp_deeplabv3plus_resnet50
# This wrapper forwards to the relocated experiment config (single source of truth).
defaults:
  - /experiment/baselines/smp_deeplabv3plus_resnet50
